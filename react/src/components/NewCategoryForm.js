// TBD:
// First, create an object structure of categories and subcategories. Is it in the right shape already? No. There is the name and then inside the array there is an Option with that top-level name.

export const NewCategoryForm = () => {
  return null;
};
